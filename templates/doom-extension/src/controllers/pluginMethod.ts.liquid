import { defineServerMethod } from '@agimon-ai/doompi-core/server-facet';
import { {{ methodName | camelCase }}Method } from '../schemas/{{ methodName | camelCase }}Method';

/** Include this declaration in the {{ scope }} scope's methods array. */
export const {{ methodName | camelCase }}Controller = defineServerMethod(
  {{ methodName | camelCase }}Method,
  async ({ value }) => ({ value }),
);
