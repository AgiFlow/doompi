/** JSON carried by the {{ methodName | camelCase }} package method. */
export interface {{ methodName | pascalCase }}Input {
  value: string;
}

export interface {{ methodName | pascalCase }}Output {
  value: string;
}
