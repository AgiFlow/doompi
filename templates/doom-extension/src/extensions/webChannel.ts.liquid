import { defineChannelFile } from '@agimon-ai/doompi-core/web';

import { {{ topic | camelCase }}Channel } from '../_lib/{{ topic | camelCase }}Store';

export default defineChannelFile({{ topic | camelCase }}Channel);
