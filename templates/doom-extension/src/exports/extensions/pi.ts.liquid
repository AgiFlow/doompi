// Pi loads the module's default export as its host entry contract, so the
// manifest-facing facade aliases the already named factory.
export {
  activate{{ packageName | replace: "doompi-", "" | pascalCase }}Extension,
  activate{{ packageName | replace: "doompi-", "" | pascalCase }}Extension as default,
} from '../../adapters/pi/extension.ts';
