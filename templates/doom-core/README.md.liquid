# {{ packageName }}

{{ packageDescription }}

Runtime systems belong in `src/systems/main` and `src/systems/child`. Put reusable services in `src/services`, plugin definitions in `src/extensions`, and integrations in `src/pi`, `src/server`, and `src/web`. Feature packages own their policies.
