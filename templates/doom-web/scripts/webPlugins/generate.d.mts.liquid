/** Hand-maintained declarations for generate.mjs; keep the two in step. */
export function renderWebPluginModules(): Map<string, string>;
export function ensureWebPluginModules(options?: { check?: boolean }): void;
