import { defineConfig } from 'tsdown';
export default defineConfig({
  entry: { index: 'src/exports/index.ts', 'bin/cli': 'src/bin/cli.ts' },
  format: ['esm', 'cjs'],
  dts: true,
  exports: false,
  unbundle: true,
  clean: true,
});
