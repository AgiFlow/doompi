import type { VariantProps } from 'class-variance-authority';
import type { ComponentProps } from 'react';
import { cn } from '../lib/cn.ts';
import { fieldVariants } from './Input.tsx';

export interface TextareaProps extends Omit<ComponentProps<'textarea'>, 'size'>, VariantProps<typeof fieldVariants> {}

/** The multi-line field, styled as the Input; `bare` drops the well for a composer that draws its own. */
export function Textarea({ className, variant, size, ...props }: TextareaProps) {
  return (
    <textarea
      data-slot="textarea"
      className={cn('resize-none', fieldVariants({ variant, size }), className)}
      {...props}
    />
  );
}
