import { defineSlotFile } from '@agimon-ai/doompi-core/web';

/**
 * Optional preview providers render launch controls here. The provider owns its
 * transient tab and preview lifecycle; Author owns the surrounding mode and
 * composer submission surface.
 */
export default defineSlotFile({});
