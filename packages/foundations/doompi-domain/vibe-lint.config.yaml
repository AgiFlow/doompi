# This package has no browser half: nothing under its (frontend) groups renders
# in a cockpit, so doom-web is not declared and no colour rule is enabled here.
plugins:
  - core
  - doom-extension

extends:
  - core/recommended
  - doom-extension/recommended

rules:
  boundary-import-allowlist: error
  file-naming-convention: error
