import { defineToolRenderer } from '@agimon-ai/doompi-core/web';

import { WriteToolMessage } from '../../../../../web/components/WriteToolMessage';

export default defineToolRenderer({ message: WriteToolMessage });
