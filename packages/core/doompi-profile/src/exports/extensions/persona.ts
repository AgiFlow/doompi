// A second Pi entry, activated by explicit subpath rather than through
// pi.extensions: detached children load the persona without the /profile command.
export { personaExtension as default, personaExtension } from '../../adapters/pi/persona.ts';
