import { defineFill } from '@agimon-ai/doompi-core/web';

import { StoryPreviewLauncher } from '../_components/StoryPreviewLauncher';

export default defineFill({ slot: 'author.preview-provider', id: 'style-system', component: StoryPreviewLauncher });
