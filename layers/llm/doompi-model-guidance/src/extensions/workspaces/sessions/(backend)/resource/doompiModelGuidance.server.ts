import { defineRoutedContribution } from '@agimon-ai/doompi-core/extension-file';

import contribution from './_lib/doompiModelGuidance.server';

export default defineRoutedContribution(contribution, {});
