import { createTeamServerSession } from '../../../../controllers/teamServerSession';

/** The session facet's contributions, as one factory, for the same reason. */
export default createTeamServerSession;
