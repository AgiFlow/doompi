import { defineSlotFile } from '@agimon-ai/doompi-core/web';

/**
 * The run detail sheet's action row, open to independent plugins.
 *
 * The filename is the local name; the path namespaces it as
 * `subagents.run-actions`, so no filler has to spell that prefix.
 */
export default defineSlotFile({});
